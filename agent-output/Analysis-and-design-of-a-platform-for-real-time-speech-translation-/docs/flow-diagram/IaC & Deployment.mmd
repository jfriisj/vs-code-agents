flowchart TD
    Dev[Developer / CI] -->|1. pulumi up| Pulumi[Pulumi Engine]

    subgraph Cloud [GCP VPC]
        Pulumi -->|2. Provision| VPC[VPC + Subnet]
        Pulumi -->|2. Provision| VM1[Manager VM]
        Pulumi -->|2. Provision| VM2[Worker VM]
        Pulumi -->|3. Configure FW| FW[Firewall Rules]
    end

    subgraph HostConfig [Cloud-init]
        Pulumi -->|Cloud-init| Docker[Install Docker Engine]
    end

    subgraph Bootstrap [Operator Runbook]
        Operator[Operator] -->|4. Use Pulumi outputs| SwarmInit[docker swarm init]
        Operator -->|5. Join workers| SwarmJoin[docker swarm join]
        Operator -->|6. Label Node| Label[Add Label: role=stateful]
    end

    subgraph Deploy [Application Deployment]
        Operator -->|7. docker stack deploy| Stack["App Stack\n(Kafka, ASR, MT...)"]
    end

    VM1 --- Docker
    VM2 --- Docker
    SwarmInit --> Label
    Label --> Stack

    style Pulumi fill:#e1bee7,stroke:#4a148c